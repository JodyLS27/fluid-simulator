#ifndef OS_EVENT_TYPES_H
#define OS_EVENT_TYPES_H

// Set maximum key count
#define EVENT_KEY_COUNT 300

namespace event {
	enum class Type
	{
		// Fill out all the buttons and their codes for the Keyboard and mouse.

		// Keyboard

		// Mouse
	};
}

#endif